# Problem:
# Ask the user:
# has_password (True/False)
# is_locked (True/False)
# If user has password and not locked, print "Access Allowed"
# Else print "Blocked"
has_password=True
is_locked=True
if has_password==True and not is_locked:
    print("Access Allowed")
else:
    print("Blocked: ")
    