#now we the help of union() we join a tuple and a set
x={1,2,3,4,5}
y=("apple","orange","grapes")
z=x.union(y)
print("set and tuple are now joined: ",z)
#operator | join only sets to sets and not allow different data types like union()
